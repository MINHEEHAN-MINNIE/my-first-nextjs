@AGENTS.md

# 프로젝트

Next.js 16 학습용 프로젝트. 최종 목표는 mediinside.ai 수준의 양면 채용 플랫폼 클론.

## 스택

- Next.js 16 (App Router) / TypeScript / Tailwind CSS
- 배포: Vercel (main 브랜치 push 시 자동 배포)
- 예정: Supabase (DB·인증·스토리지), Claude API, 토스페이먼츠

## 파일 구조

- 페이지는 `src/app/` 아래에 있다 (`app/`이 아님)
- 폴더 경로 = URL 경로, 폴더 안의 `page.tsx`가 그 주소의 화면

# 나에 대해

기획 10년 · 디자인 5년. **코딩은 배우는 중이다.** 30일 커리큘럼으로 학습 중이며,
목표는 코드를 받는 게 아니라 **직접 만들 수 있게 되는 것**이다.

# 응답 규칙

1. **한국어로 설명한다.**
2. **코드를 쓰기 전에 무엇을 왜 하는지 먼저 설명한다.** 결과만 던지지 않는다.
3. **한 번에 하나씩 한다.** 여러 기능을 한꺼번에 만들지 않는다.
4. 파일을 여러 개 고쳐야 하면 **먼저 계획을 말하고 확인을 받는다.**
5. 전문 용어를 처음 쓸 때는 짧게 뜻을 붙인다.
6. 내가 "어떻게 해?"라고 물으면 **방법을 설명하고, 코드는 내가 직접 치게 둔다.**
   "해줘"라고 하면 그때 대신 작성한다.
7. 파괴적인 git 명령(`reset --hard`, `push --force`, 브랜치 삭제)은 **실행 전에 반드시 확인을 받는다.**

# 커밋

- 메시지는 한국어, 무엇을 왜 바꿨는지 한 줄
- 작동하는 상태가 되면 즉시 커밋한다

# 참고 문서

- `docs/errors.md` — 지금까지 만난 에러와 원인·해결 기록. 새 에러를 해결하면 여기에 추가한다.
- `docs/new-project-checklist.md` — 새 프로젝트를 만들어 배포까지 가는 순서
